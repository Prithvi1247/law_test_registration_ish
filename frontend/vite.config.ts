import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    // Proxies /api/* to the FastAPI backend in local dev so the frontend
    // can call relative paths like POST /api/applicants.
    // Adjust the target to wherever the FastAPI server actually runs.
    proxy: {
      "/api": {
        target: "http://localhost:8000",
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ""),
      },
    },
  },
});
