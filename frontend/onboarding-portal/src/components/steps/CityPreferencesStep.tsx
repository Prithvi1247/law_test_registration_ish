import { NotImplementedStep } from "./NotImplementedStep";

// Section 4 — City Preferences. Will use GET /test-centres (state -> city
// dependent dropdown) and POST /applicants/{applicant_id}/test-selection.
export function CityPreferencesStep() {
  return <NotImplementedStep title="City Preferences" />;
}